# dependencies
node_modules/
.pnpm-store/

# next
.next/
out/
# env / secrets (.env.example stays versioned)
.env
.env.local
.env.development
.env.test
.env.production
.env.*.local
# Cursor MCP local (may reference secrets; use mcp.example.json in git)
.cursor/mcp.json
# logs / debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
*.log
# OS / IDE
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json
# test / coverage
coverage/
playwright-report/
test-results/
blob-report/
playwright/.auth/
# prisma
prisma/*.db
prisma/*.db-journal
# pilot reset logical backups (may contain lead PII)
backup/
# typescript
*.tsbuildinfo
# Playwright MCP local artifacts
.playwright-mcp/
# playwright mcp
# node

# Vercel CLI local link (no secrets; still machine-local)
.vercel

# Hygiene dry-run reports (may contain lead PII)
hygiene-e2e-leads-report-*.json
.env.production.local.dryrun
