---
description: UI do pipeline, lead detail e formulários
globs: src/app/**/*.{ts,tsx},src/components/**/*.{ts,tsx},src/features/**/*.{ts,tsx}
alwaysApply: false
---

# Frontend

- Priorizar o fluxo vertical: lista/pipeline → lead detail → handoff → modal de atividade + próximo passo.
- Filtros V1: stage, owner, follow-ups atrasados, **parados** (definição em `docs/product.md`).
- CTA de canal deve deixar claro que abre WhatsApp/e-mail; em seguida exigir registro de resultado.
- Não construir dashboard/gráficos na V1.
- Desktop-first para sócios `MEMBER`; utilizável no celular.
- Ações só `ADMIN` ocultas/desabilitadas com clareza; server é a barreira.
- Server Components por padrão; Client só com interação.
- **Padrão UI:** Chakra UI v3 ([ADR 0011](../../docs/adr/0011-ui-stack-keep-tailwind.md)) — `src/theme/`, `src/components/ui/`, Provider no root layout.
- **Legado:** Tailwind só em telas ainda não migradas; ao tocar uma tela, preferir Chakra.
- Sem estado global prematuro; sem big-bang (não apagar Tailwind de uma vez).
- Skills `chakra-ui-*` são tooling primário de UI; sem charts/dashboard vanity na V1.
